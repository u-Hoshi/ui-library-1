'use client';

import { Button } from '@/src/components/atoms';

export default function Home() {
  return (
    <Button
      text="Hello"
      onClick={() => {}}
      color="primary"
    />
  );
}
